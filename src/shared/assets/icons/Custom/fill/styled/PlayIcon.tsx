import { FC } from 'react';
import { IIcon } from "@/shared/assets/icons/Custom";

// import PlaySvg from '@/shared/assets/icons/svg/fill/fill_play.svg';

export const PlayIcon: FC<IIcon> = ({ className, alt }) => {
  // return <Icon icon={PlaySvg} className={className} alt={alt} />;
  return <></>
};
