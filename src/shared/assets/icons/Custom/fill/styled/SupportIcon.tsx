import { FC } from 'react';
import { IIcon } from "@/shared/assets/icons/Custom";

// import SupportSvg from '@/shared/assets/icons/svg/fill/fill_support.svg';

export const SupportIcon: FC<IIcon> = ({ className, alt }) => {
  // return <Icon icon={SupportSvg} className={className} alt={alt} />;
  return <></>
};
