import { FC } from 'react';
import { Icon, IIcon } from "@/shared/assets/icons/Custom";

// import MediaSvg from '@/shared/assets/icons/svg/fill/fill_media.svg';

export const MediaIcon: FC<IIcon> = ({ className, alt }) => {
  // return <Icon icon={MediaSvg} className={className} alt={alt} />;
  return <></>
};
